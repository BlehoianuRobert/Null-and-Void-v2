# Copy to `docker-compose.override.yml` (same folder as docker-compose.yml).
# Docker Compose merges this file automatically.
#
# Main docker-compose.yml already defaults mqtt-worker DEVICE_SERIAL to 98:A3:16:7E:57:C0.
# Use this file only if you need a different MAC or extra env without editing the main compose.

services:
  mqtt-worker:
    environment:
      # DEVICE_SERIAL: "AA:BB:CC:DD:EE:FF"
      # DEVICE_API_KEY: "paste-same-as-web-DEVICE_API_KEY"
